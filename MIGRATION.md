# 光影写手 v1.3.0 源码回迁历史

本目录是从早期源码创建的独立迁移副本。本文件仅记录历史背景；公开项目与新安装包统一使用「光影写手 / Guangying Writer」名称。

## 原则

- 每次打包使用新的版本目录和版本号。
- 先保留 v1.2.8 的行为，再做设计优化；不能以重做界面为由删除已有能力。
- `.zhsp` 项目文件必须向后兼容；新字段应当是可选字段。
- 不提交 `node_modules`、构建输出、发布 App 或用户剧本文件到 GitHub。
- `src/model/sample.ts` 必须保持为空白初始稿。禁止把用户剧本、真实人物名、剧情梗概或测试工程作为示例提交或打包。

## 2026-09-06 接手状态

- 历史阶段曾以独立旧版本做最小补丁；这些本机成品不属于本仓库，也不会被新版本覆盖。
- v1.2.9 已实现并验证：统计标题层级、统计页角色改名并同步正文、自由板连线备注、三位数目标页数显示、按“已写页数 / 目标页数”移动的打字机进度指针。
- 历史成品补丁、CSS、测试和截图仅存在于本机留档，不会进入 GitHub 克隆。
- 本源码仓库的 `v1.3.0-alpha` 已有上述功能的源码实现，但仍不等价于完整 v1.2.8/v1.2.9；不要把 alpha 包替换成用户日常使用版。
- 成品版使用 PNG 打字机指针；源码 alpha 的 `StatusBar.tsx` 仍是字符 `⌄`，后续应改用正式图片资源并补视觉回归。
- 继续维护前先阅读仓库中的 `DEVELOPER_HANDOFF.md` 与 `CORE_FEATURES.md`。

### Git 历史隐私提醒

当前工作树已经移除具体示例正文，但旧提交可能仍保留旧版 `src/model/sample.ts`。普通新增提交不能清除历史内容。若该文本不应继续公开，接手者必须先备份仓库，再经用户明确同意后重写历史并 force-push；这是高风险操作，不能自行执行。

## 已确认需要回迁的能力

1. 自由板卡片：完整内容、更紧凑的布局、可缩放及尺寸持久化。
2. 自由板关系线：卡片间创建、删除和持久化连接。
3. 写作辅助：Tab 在常规文本/备忘间切换；连续说话者按正确条件显示 `CON'T`。
4. 写作素材：图片卡和声音卡归入目标页数区域，保留添加、编辑、定位与删除。
5. 进度：按场景目标篇幅显示可切换的香烟、汽车、钥匙主题进度条。
6. 界面：可收起的左侧导航、文件菜单不被遮挡，以及卡片/素材视觉修正。

## 回归门槛

- TypeScript 检查和生产构建通过。
- 启动后的基础烟雾测试通过，控制台无页面错误。
- 对保存、重新打开、导出、自由板、进度和写作快捷键做人工/自动回归。
- macOS arm64 包独立输出；只在验证后作为 v1.3.0 候选版交付。
