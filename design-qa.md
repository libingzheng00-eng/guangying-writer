# 日间工作台视觉核对

## 比对对象与状态

- 用户视觉目标：
  - `/var/folders/ln/9kx_scj10zd4zmbznrs9jmj40000gn/T/codex-clipboard-ee408966-4b87-4608-a992-280de47648c9.png`（自由板日间状态，含深色元素类型框和关系备注问题）。
  - `/var/folders/ln/9kx_scj10zd4zmbznrs9jmj40000gn/T/codex-clipboard-bf983461-1caf-42b8-bea5-8d4a9b068da6.png`（设置弹窗日间状态）。
- 实现截图（Chrome 本地预览、日间模式、756 × 413 CSS 像素）：
  - `/private/tmp/guangying-ui-alpha12-day.png`：写作页与进度区。
  - `/private/tmp/guangying-settings-alpha12-day.png`：设置弹窗。
  - `/private/tmp/guangying-board-note-alpha12-day.png`：自由板关系备注。
- 所有截图按同一浏览器密度采集；示例工程内容不同，仅比较控件结构、颜色与层级。

## 比较与修正

- [P1，已修正] 日间模式的元素类型选择框仍是黑色。
  - 证据：实现截图中该选择框为白色背景、深灰文字。
  - 修正：为 `.type-select` 加入日间表面、边框与文字颜色。
- [P1，已修正] 自由板关系备注仍是黑色悬浮条。
  - 证据：实现截图中备注条为白灰底、深色文字和低对比边框。
  - 修正：为 `.board-link-note`、输入框和删除按钮补齐日间样式。
- [P1，已修正] 设置弹窗及表单仍显示夜间黑色表面。
  - 证据：实现截图中弹窗、标题、标签、输入框和遮罩均为日间状态。
  - 修正：为弹窗、表单、标签和按钮补齐日间规则。
- [P2，已修正] 当前页文案冗余、右侧目标页未与两组素材控制对称。
  - 证据：实现截图仅显示 `1/100`；目标页数位于两个素材控制组的正上方。
  - 修正：移除“当前页”和单位文案，工具区改为居中纵向布局。
- [P2，已修正] 场景色带颜色偏灰绿，与日间纸面不协调。
  - 证据：实现采用统一雾蓝十阶配色，深浅模式中保持可读。

## 必要表面检查

- 字体与文案：日间控件均为深灰正文、蓝色激活态；当前页仅保留数字关系。
- 间距与布局：目标页数在素材控制正中上方，左右信息在进度区垂直居中。
- 颜色与令牌：选择框、关系备注、弹窗及场景色带均已使用日间令牌。
- 图像与素材：未新增或替换图像资产；原有图片卡内容与裁切逻辑保持。
- 可用性：目标页输入、素材显隐/新建、关系备注编辑、设置标签页均保持原行为。

## 最终结果

final result: passed

## alpha.13 进度条与板块视觉核对

- 参考截图：
  - `/var/folders/ln/9kx_scj10zd4zmbznrs9jmj40000gn/T/codex-clipboard-4444c70c-193a-4fd5-a60a-d18499259cd7.png`
  - `/var/folders/ln/9kx_scj10zd4zmbznrs9jmj40000gn/T/codex-clipboard-93d6e974-da3d-4ff9-8c6c-6ec71218be71.png`
  - `/var/folders/ln/9kx_scj10zd4zmbznrs9jmj40000gn/T/codex-clipboard-20726e9d-c0dc-434e-b22c-0ddc117a2ea1.png`
- 本地 Chrome 实机截图：
  - `/private/tmp/guangying-alpha13-writing-day.png`
  - `/private/tmp/guangying-alpha13-board-day-v2.png`
- 已核对：当前页仅显示 `1/100`，手写风格数字跨进度轨道两行居中；自由板顶部三分区和颜色工具区已进入 DOM 并使用日间对比度；卡片关联选择区和关系备注样式已按标注图调整；故事板卡片尺寸规则已放大。
- 代码级验证：typecheck、build、render、writing、zhsp 兼容、history 全部通过。
- 备注：Chrome 隔离页中的空白样本没有关系线和灵感卡，因此关系备注与卡片浮动按钮的最终像素位置仍需在含真实自由板卡片的工程中复核；不会读取用户剧本来做此注入。

## alpha.14 日间可读性与自由板选择核对

### 比对对象

- 用户视觉目标：
  - `/var/folders/ln/9kx_scj10zd4zmbznrs9jmj40000gn/T/codex-clipboard-ff98b052-5139-48fd-91e1-cf86bb4afc8d.png`（日间模式下顶栏、统计、自由板按钮、颜色栏及侧栏顶部对齐问题）。
- 实现截图（本机隔离 Chrome，日间模式，1440 × 900 CSS 像素，密度 1）：
  - `/private/tmp/guangying-alpha14-writing-day.png`
  - `/private/tmp/guangying-alpha14-stats-day.png`
  - `/private/tmp/guangying-alpha14-board-recolored.png`
- 状态：空白样本工程；自由板中单击选中场景卡，再点第一枚颜色按钮。

### 比较历史与修正

- [P1，已修正] 侧栏标签页被收起把手错误占据的 42px 高度向下挤开。
  - 证据：旧截图在侧栏标签前有整行空白；新版写作截图中「导航 / 大纲 / 属性」从主区域顶线直接开始。
  - 修正：恢复 `.sidebar__collapse` 的绝对定位，并单独保留收起后的 `.sidebar__reveal` 左侧位置。
- [P1，已修正] 自由板视觉选中与颜色功能读取不同的卡片 ID，导致已选卡仍显示“先选中卡片”。
  - 证据：隔离预览中选中后 `.bcard.is-selected` 数量为 1，8 枚颜色按钮均为 `disabled: false`；点色后选中场景卡的 `--scene-color` 更新为 `#cfe4ff`。
  - 修正：单击、多选、范围选和框选统一为 `scene:` / `beat:` 选择 ID；同时兼容旧内存中的无前缀值。
- [P1，已修正] 日间统计页、主视图切换和右上工具栏文字过浅或遗留深色按下态。
  - 证据：新版统计截图中数字、标题、表格和说明均为深色；顶部激活态为白底蓝字，撤销 / 重做 / 保存 / 文件 / 设置使用同一浅色工具栏规则。
  - 修正：增加日间专用文字、悬停、聚焦和按下状态，不改变夜间模式。
- [P2，已修正] 自由板工具栏过宽、颜色栏偏离中心，三分区文字可见性不足。
  - 证据：新版 DOM 与实际点击验证确认「总览 / 场景板 / 灵感板」三个标签均存在且可点击，颜色栏位于工具栏中列；日间模式采用明确分段底色与深色文字。
  - 修正：工具栏改为 `auto / 1fr / auto` 三列，中心工具强制居中，右侧加卡与缩放控件收窄。

### 必要表面检查

- 字体与文案：统计页数字、标题、表格列头与顶栏文本均有清晰层级；当前页仍保留之前确定的手写风格 `1/100`。
- 间距与布局：侧栏顶部空隙清除；自由板工具区保持左分区 / 中央颜色 / 右操作的节奏。
- 颜色与令牌：日间模式不再继承顶栏深底；选中态使用蓝色但不依赖发光效果。
- 图像与素材：本轮未添加、替换或伪造图片资产。
- 文案与交互：三分区、选中、改色、双击场景卡进入写作页均保留可用；空白样本不含用户剧本。

### 残余限制

- 自由板截图采集时 Chrome 的局部合成对文字有不稳定表现，而 DOM 尺寸、命中与点击状态正常；因此卡片浮层与关系备注在真实 Electron 试用中的像素级视觉仍需复核。

## 最终结果

final result: blocked

## alpha.15 快捷输入与写作节奏核对

### 本轮目标

- 修复自由板「总览 / 场景板 / 灵感板」在日间模式中只留下颜色圆点、文字不易发现的问题。
- 将快捷输入候选从稿纸左侧的错误位置移至当前输入行右侧，并让候选能由鼠标、空格或回车确认。
- 确认写作默认节奏为：场次标题后先写动作，人物后进入对白，对白后进入人物；Tab 只切换当前行类型。
- 为打字机指针增加仅在连续输入期间存在的低幅度节奏动效。

### 已检查的实现

- [P1，已修正] 自由板三分区在日间模式改为显式分段按钮，使用可读的深色文字、边框与浅灰表面；颜色工具仍保持中列居中。
- [P1，已修正] 快捷输入候选改为真实 `button`，使用固定视口坐标紧贴当前行右侧；日间模式使用浅色表面、深色文字与清晰焦点态。鼠标按下直接更新 contentEditable DOM 和工程状态，避免“点击无反应”。
- [P1，已修正] 回车与 Tab 逻辑由纯函数回归测试覆盖：场次标题 → 动作、动作 → 动作、人物 → 对白、对白 → 人物；Tab 循环不创建新行。
- [P2，已修正] 指针动效仅由临时 `isTyping` 状态控制，停键 560ms 后停止，并提供 `prefers-reduced-motion` 降级；不写入工程文件或撤销历史。

### 验证与残余限制

- `typecheck`、`build`、渲染检查、写作逻辑、`.zhsp` 兼容与撤销历史检查均通过；测试数据为合成工程。
- Chrome 隔离预览此前对自由板局部文字存在合成显示不稳定，不能替代新安装包中的 Electron 像素级确认。因此 alpha.15 的三分区可见性、候选菜单位置和指针动效仍需在新独立包里实际打开后复核。

## 最终结果

final result: blocked

## alpha.16 自由板工具栏与全局色系核对

### 本轮目标

- 解决自由板的「总览 / 场景板 / 灵感板」和颜色切换功能虽然存在、却被背景层遮住而不可见的问题。
- 将输入停止后打字机指针的节奏微动缩短到约 0.26 秒，并避免它带动整个编辑器重绘。
- 收敛深色 / 日间模式的控制表面、文字对比与背景特效，减少颜色割裂和不必要绘制。

### 实际界面核对

- 2026-09-07 在本机隔离源码预览（`127.0.0.1:4180`、1280px 宽）中核对：顶部自由板工具栏可见，左侧显示「总览 / 场景板 / 灵感板」，中间显示 8 枚颜色按钮，右侧显示加卡和缩放控制。
- 实际点击：依次切换三块子板均切换选中态；选中场景卡后「卡片颜色 3」由禁用变为可点，点击后选中态保持。
- 本次是浏览器中的隔离源码界面核对，不等同于新安装包的 Electron 首次启动验收；没有读取或写入用户剧本。

### 代码与性能核对

- `.board__bar` 提升到工作区背景层之上；`.app-body` 作为稳定内容层，避免同类 z-index 回归。
- 指针状态由 `ProgressBar` 直接操作单一 DOM 节点，`Editor` 不再为短暂动效更新 React state；系统“减少动态效果”时仍关闭动画。
- 删除无调用的汽车 / 钥匙进度主题代码、旧 SVG 与旧装饰样式；修复批量删除处理真实 `beat:` 选择 ID。
- `typecheck`、生产构建、渲染、写作、`.zhsp` 兼容、撤销历史及 `noUnusedLocals/noUnusedParameters` 检查通过。

## 最终结果

final result: passed
