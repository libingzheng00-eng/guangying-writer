# 写作区布局视觉核对

## 比对对象

- 视觉目标：`/var/folders/ln/9kx_scj10zd4zmbznrs9jmj40000gn/T/codex-clipboard-fa3cbf41-d41e-4fd7-9ac3-26d3f7e94e1b.png`（2048 × 1158，日间模式、含用户标注）。
- 实现截图：`/private/tmp/mochang-ui-r6-night-2.png`（1440 × 900，Chrome 本地预览、深色模式）。
- 状态：写作页、空白测试工程；实现截图的目标页数为 100，场景色带和素材控制均已显示。

两张图主题与示例内容不同，不能把色彩或卡片位置作逐像素比对；本次只核对用户标注的结构与几何关系。

## 迭代记录

### 第 1 轮

- [P1] 目标页数和素材按钮处于同一水平行，右侧层级混乱。
- [P1] 侧栏收起按钮在左侧边缘，尺寸过大。
- [P2] 场景色带上方有“已写 …”提示，干扰阅读；日间模式未覆盖侧栏、进度区和状态栏。

### 修正后核对

- 目标页数已改为右侧工具区的上行，两组素材控制在其下；当前页信息和右侧工具整体均在进度区垂直居中。
- 深色模式截图确认：场景色带已无居中文字，宽度与上方纸带轨道一致；侧栏按钮已缩小并位于侧栏右边缘中部。
- 日间模式已补齐对应选择器，但本轮没有取得与目标同一日间状态的浏览器截图，仍须在打包 app 中肉眼确认。

## 必要表面检查

- 字体与文案：标题、当前页、目标页和素材计数保持现有文本层级；无额外进度提示字。
- 间距与布局：右侧工具改为纵向层级；卡片宽度、内边距、阴影均已收紧。
- 色彩：深色实拍通过；日间颜色规则已补齐但缺少同状态实拍。
- 图像与素材：未替换或生成图像资产；图片卡保留原图比例和圆角。
- 可用性：目标页数输入、素材显示/新建按钮、收起侧栏和场景跳转的语义未改变。

## 后续人工验收

1. 在日间模式打开写作页，确认侧栏、进度区、底部状态栏均为浅色。
2. 点击侧栏把手，确认它始终在侧栏右缘中部，收起后再次点击可展开。
3. 新建一张声音卡和图片卡，确认紧凑卡片仍可编辑、拖动和删除。

## 最终结果

final result: blocked

阻塞原因：无法在本轮本地浏览器中取得与用户目标截图相同的日间模式、相同内容状态的实拍，因此不能把日间视觉验收标记为通过。
