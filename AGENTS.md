# 光影写手：所有维护者与 AI 的项目约定

修改前完整阅读 `CORE_FEATURES.md` 与 `DEVELOPER_HANDOFF.md`。用户当前明确要求高于历史设计决定；不得把已废止的素材文末附页恢复回来。

## 不可随意改变的行为契约

- 九类 Tab / Shift+Tab 必须在同一正文元素内闭环；不是浏览器控件焦点导航。不得以简化代码为由删除普通文本、备忘或焦点恢复。
- 创作 PDF 必须保留写作画布实际图文位置，场景旁边的图仍在该场景旁边；A4 纯文本是另一条明确出口，不含卡片。不许把图片统一挪到文末。
- 本地图片拖入、声音/图片卡、Shift 多选、故事板归幕、自由板连线与备注、删除整场、撤销恢复、保存兼容均为核心能力。不得用“优化”名义删入口或换语义。
- 正常功能修复允许修改核心源码；保护的是行为，不是文件哈希。需要改变核心契约时，先向用户说明影响并取得明确认可，补测试与更新文档后才可实施。

## 隐私与发布

- 不读取、修改或提交用户正在使用的剧本、自动保存和导出稿，也不复制它们用作测试。验收只使用新建的合成测试数据和独立临时 userData。合成数据可以保留在测试源码中。
- 不随意改变 `productName`、应用标识、数据目录或存储键。旧工程格式和旧存储键属于兼容层，不可因为旧品牌名称就删除；跨数据目录迁移必须单独设计、备份和验证。
- 安装/更新前保留用户现用应用与数据，不以进程强杀替代安全保存。打包只取生产文件；`.app`、DMG、ZIP 上传 Releases，不提交到源码仓库。
- 不批量删除文件，不重写 Git 历史，不 force-push，不修改仓库权限/分支规则，除非取得对应明确授权。

## 必须验证

执行 `CORE_FEATURES.md` 的完整基线，不能只跑纯函数测试便宣布鼠标/键盘/PDF 实机验收通过。发布包另跑 `node scripts/core-guard.cjs --package /明确路径/Contents/Resources/app`，检查新用户首次启动、实际 PDF、包内容和签名状态。

CI 是回归报警器，不是不可绕过的代码锁。只有仓库管理员配置为必需状态检查/受保护分支后，才能阻止未通过检查的合并；目前不得宣称已配置远端强制规则。
